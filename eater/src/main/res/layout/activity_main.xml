<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <!-- android:clipToPadding="true" -->
    <!-- android:fitsSystemWindows="true" -->

    <View
        android:id="@id/statusbar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/status_bar_height"
        android:background="@color/color_primary" />

    <include layout="@layout/toolbar" />
    
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@id/mainActivity_tv_tips"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:layout_margin="@dimen/activity_horizontal_margin"
            android:text="@string/no_order"
            android:textColor="@color/text_black_main"
            android:textSize="@dimen/abc_text_size_title_material"/>

        <ListView
            android:id="@id/mainActivity_lv_menu"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <Button
            android:id="@id/mainActivity_btn_flat"
            android:layout_width="@dimen/button_flat"
            android:layout_height="@dimen/button_flat"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"
            android:layout_alignParentEnd="true"
            android:layout_margin="@dimen/activity_horizontal_margin"
            android:background="@mipmap/icon_add"
            android:elevation="1.5dp"/>

    </RelativeLayout>

</LinearLayout>