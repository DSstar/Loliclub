<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.


    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.NoActionBar">

        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.


        -->
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <!-- toolbar（actionbar）颜色 -->
        <item name="colorPrimary">@color/primary</item>
        <!-- 状态栏颜色 -->
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <!-- 窗口的背景颜色 -->
        <item name="android:windowBackground">@android:color/white</item>
        <!-- SearchView -->
        <item name="searchViewStyle">@style/MySearchViewStyle</item>

        <!--切换时的动画效果-->
        <!--<item name="android:windowAnimationStyle">@style/AnimationActivity</item>-->
        <!--spinner-->
        <item name="android:spinnerStyle">@style/MySpinnerStyle</item>
        <item name="android:spinnerItemStyle">@style/MySpinnerItemStyle</item>
        <item name="android:spinnerDropDownItemStyle">@style/MyDropDownItemStyle</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="welcomeTheme" parent="android:Theme.NoTitleBar"></style>

    <style name="MySearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!--
    Background for the search query section (e.g. EditText)
    <item name="queryBackground">...</item>  
    Background for the actions section (e.g. voice, submit)  
    <item name="submitBackground">...</item>  
    Close button icon  
    <item name="closeIcon">...</item>  
    Search button icon  
    <item name="searchIcon">...</item>  
    Go/commit button icon  
    <item name="goIcon">...</item>  
    Voice search button icon  
    <item name="voiceIcon">...</item>  
    Commit icon shown in the query suggestion row  
    <item name="commitIcon">...</item>  
    Layout for query suggestion rows  
    <item name="suggestionRowLayout">...</item>
        -->
    </style>

    <style name="MyCustomDialog" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">false</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <!--APP切换动画-->
    <style name="AnimationActivity" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_bottom</item>
        <item name="android:activityOpenExitAnimation">@null</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_in_bottom</item>
        <item name="android:activityCloseExitAnimation">@null</item>
    </style>

    <!--spinnery样式-->
    <style name="MySpinnerStyle" parent="Base.Widget.AppCompat.Spinner">
        <item name="android:dropDownSelector">@drawable/abc_spinner_mtrl_am_alpha</item>
    </style>

    <style name="MySpinnerItemStyle">
        <item name="android:textColor">@color/text_black_sub</item>
        <item name="android:textSize">@dimen/abc_text_size_subhead_material</item>
        <item name="android:gravity">center_horizontal|center_vertical</item>
    </style>

    <style name="MyDropDownItemStyle" parent="@style/Widget.AppCompat.DropDownItem.Spinner">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/text_black_main</item>
        <item name="android:textSize">@dimen/abc_text_size_subhead_material</item>
        <item name="android:paddingStart">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingEnd">@dimen/activity_horizontal_margin</item>
        <item name="android:minWidth">@dimen/button_raised_minwidth</item>
        <item name="android:gravity">left|center_vertical</item>
    </style>

</resources>